export type DashboardData = { sites: number; roomsThisWeek: number; ticketsOpen: number };
export type Project = { id: string; site: string; name: string; phase: string; owner: string; due: string };
export type Ticket = { title: string; priority: string; status: string };


