// Example Firebase configuration file
// Copy this file to firebase-config.js and fill in your actual Firebase project values

window.FIREBASE_CONFIG = {
  // Required fields for Firebase Auth to work:
  apiKey: "your-firebase-api-key-here",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  
  // Optional fields (can be undefined if not needed):
  appId: "your-firebase-app-id",
  messagingSenderId: "your-messaging-sender-id",
  storageBucket: "your-project-id.appspot.com"
};

// To get these values:
// 1. Go to your Firebase Console: https://console.firebase.google.com/
// 2. Select your project
// 3. Click the gear icon (⚙️) next to "Project Overview"
// 4. Select "Project settings"
// 5. Scroll down to "Your apps" section
// 6. Click on your web app or create a new one
// 7. Copy the config values from the provided code snippet
